# 本地开发配置示例
# 复制此文件为 .env 并填入你的实际 Token

# ============================================
# 主 API Token（必需）
# ============================================
SENSOR_TOWER_API_TOKEN=st_your_primary_token_here

# ============================================
# 备用 API Token（可选但推荐）
# ============================================
# 当主 Token 配额用完时自动切换到此 Token
# 建议使用不同的配额计划以确保高可用性
SENSOR_TOWER_API_TOKEN_BACKUP=st_your_backup_token_here

# ============================================
# 服务器配置
# ============================================
# API 基础 URL（通常不需要修改）
API_BASE_URL=https://api.sensortower.com

# 传输模式: stdio（用于 MCP 客户端）或 http（用于 HTTP 服务器）
TRANSPORT=stdio

# HTTP 服务器端口（仅在 TRANSPORT=http 时使用）
PORT=8666

# ============================================
# 测试配置
# ============================================
TIMEOUT=60

# ============================================
# Docker 配置
# ============================================
COMPOSE_PROJECT_NAME=sensortower-mcp

# ============================================
# 使用说明
# ============================================
# 1. 获取 API Token:
#    访问 https://app.sensortower.com/users/edit/api-settings
#
# 2. 配置主 Token:
#    将 SENSOR_TOWER_API_TOKEN 设置为你的主 Token
#
# 3. 配置备用 Token（推荐）:
#    将 SENSOR_TOWER_API_TOKEN_BACKUP 设置为你的备用 Token
#    这样当主 Token 配额用完时会自动切换
#
# 4. 启动服务器:
#    python -m sensortower_mcp.server
#
# 5. 查看日志:
#    启动时会显示配置的 Token 数量
#    切换时会显示: ⚠️  Switching to backup token #2
